// import { EmojiIdentifierResolvable, MessageReaction, PartialUser, User } from 'discord.js';

export class CommandProcessor {
  // processMessage(cmd: string): void {
  //
  // }
  //
  // processReaction(reaction: MessageReaction, user: User | PartialUser): void {
  //
  // }
  //
  // getReactions(): EmojiIdentifierResolvable[] {
  //
  // }
}